{
  "name": "internal-ad-tracking",
  "version": "1.0.0",
  "private": true,
  "workspaces": [
    "api",
    "pixel",
    "dashboard"
  ],
  "scripts": {
    "dev:api": "npm run dev --workspace=api",
    "dev:pixel": "npm run dev --workspace=pixel",
    "build": "npm run build --workspaces",
    "setup:shopify": "tsx scripts/setup-shopify-client.ts",
    "setup:stripe": "tsx scripts/setup-stripe-client.ts",
    "migrate": "npm run migrate --workspace=api",
    "sync:ad-costs": "npm run sync:ad-costs --workspace=api"
  },
  "devDependencies": {
    "tsx": "^4.16.2",
    "typescript": "^5.5.4",
    "uuid": "^11.1.1",
    "pg": "^8.12.0",
    "dotenv": "^16.4.5",
    "@types/pg": "^8.11.6",
    "@types/uuid": "^10.0.0",
    "@types/node": "^22.0.0"
  }
}
